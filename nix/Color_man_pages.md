Dar color a man:

```
Ubuntu/Debian-based:
$ sudo apt install most

OpenSUSE:
$ sudo zypper in most

Agregar a profile
$ echo 'export PAGER="/usr/bin/most -s"' >> ~/.zshrc
O
$ echo 'export PAGER="/usr/bin/most -s"' >> ~/.bashrc
```

